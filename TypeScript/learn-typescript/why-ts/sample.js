// function sum(a,b){
//   return (a + b)
// } 

// sum(10, "20") // "1020";

// 자바스크립트에서 타입스크립트처럼 사용하기

// @ts-check
// -> ts같은 효과를 보여준다.
/**
 * 
 * @param {number} a 
 * @param {number} b 
 */

function sum(a, b) {
  return (a + b)
}

sum(10,'20')

